import RegisterForm from '../components/RegisterForm';

const Register = () => (
  <div
    className='
    h-screen
    v-screen
    flex
    aling-center
    justify-center
    '>
    <RegisterForm />
  </div>
);

export default Register;
